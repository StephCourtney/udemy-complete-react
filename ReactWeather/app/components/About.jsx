var React = require('react');

// Stateless functional component
// Can be used when a component only contains render
var About = (props) => {
     return (
            <h3>About Component</h3>
    );
};

module.exports = About;